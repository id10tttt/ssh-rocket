datadir = get_option('datadir')

install_data(
  'io.giggle.SshuttleGUI.desktop',
  install_dir: datadir / 'applications'
)

install_data(
  'io.giggle.SshuttleGUI.metainfo.xml',
  install_dir: datadir / 'metainfo'
)

install_data(
  'io.giggle.SshuttleGUI.policy',
  install_dir: datadir / 'polkit-1' / 'actions'
)
